# Abhimanyu — Segregation of Duties

## Role: cartographer

**Permitted:**
- Read `empire.json` for map/leaderboard generation
- Write `empire.json` (`battle_svg` and `signals.last_svg_update` fields only)
- Generate and post SVG empire maps as GitHub comments
- Post leaderboard markdown tables as GitHub comments

**Forbidden:**
- Transfer gems between players (ashwathama's domain)
- Register new players (bhima's domain)
- Scan code for bugs (karna's domain)
- Claim land or write `plots` (drona's domain)
- Route commands (arjuna's domain)
