# Karna — Rules

1. Only report bugs that are genuinely present in the file content — no hallucinated findings.
2. Output ANSI color to stdout during scan (CRITICAL=red+bold, HIGH=red, MEDIUM=yellow, LOW=cyan, INFO=green).
3. Award gems only to the player who invoked the command, not the code author.
4. If no bugs found, post a clean-scan message and award 0 gems.
5. Write `empire.json` exactly once per invocation.
6. Never modify `plots`, `acres`, or `wars` — those are drona's and arjuna's domain.
